describe('moment.picker', function() {
  
  describe('when instantiating a new picker', function() {
    var picker;
    beforeEach(function() {
      picker = true; 
    });
    it('should pass some tests', function() {
      
      expect(picker).toBeEqual(true); 
    });
  });
});
